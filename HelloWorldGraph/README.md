# Goals/Requirements
- Learn how to use AgentState
- Create a few node functions to practice updating state
- Setup a basic LangGraph file (I think?) structure
- Compile and invoke a LangGraph graph
- Figure out data flow through one node

## Notes

We're basically just making a single node graph.
So: Start -> Node -> End, and thats it.

### Docstrings

Docstrings are super important in LangGraph because they 
will tell the AI agents what that function (node) actually does
